# DROP Command in MySQL

The `DROP` command is a **DDL (Data Definition Language)** command.

It is used to **permanently remove a database object**.

Common objects that can be dropped are:

-   Tables
-   Databases
-   Views
-   Indexes
-   Primary keys and foreign keys using `ALTER TABLE`

> **Warning:** `DROP` is destructive. The specified object and its
> associated data are permanently removed.

------------------------------------------------------------------------

# 1. DROP TABLE

`DROP TABLE` permanently removes a table and all data stored in it.

## Syntax

``` sql
DROP TABLE table_name;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_table_db;

USE drop_table_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

-- View data before DROP
SELECT * FROM student;

-- View tables
SHOW TABLES;

-- Drop the table
DROP TABLE student;

-- View tables after DROP
SHOW TABLES;
```

After:

``` sql
DROP TABLE student;
```

the table no longer exists.

Trying:

``` sql
SELECT * FROM student;
```

will produce an error because the table has been removed.

## Using IF EXISTS

``` sql
DROP TABLE IF EXISTS student;
```

This prevents an error if the table does not exist.

------------------------------------------------------------------------

# 2. DROP DATABASE

`DROP DATABASE` permanently removes a database and everything inside it.

## Syntax

``` sql
DROP DATABASE database_name;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_database_db;

USE drop_database_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'MCA');

SELECT * FROM student;

SHOW TABLES;

-- Drop the complete database
DROP DATABASE drop_database_db;
```

The following are removed together:

``` text
Database
    |
    +-- Tables
    |     |
    |     +-- Data
    |
    +-- Other database objects
```

## Using IF EXISTS

``` sql
DROP DATABASE IF EXISTS drop_database_db;
```

> **Warning:** `DROP DATABASE` permanently removes the database and
> everything inside it.

------------------------------------------------------------------------

# 3. DROP VIEW

A **view** is a virtual table based on a query.

`DROP VIEW` removes the view.

> Dropping a view does **not** delete the original tables used by the
> view.

## Syntax

``` sql
DROP VIEW view_name;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_view_db;

USE drop_view_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30),
    Marks INT
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA', 85),
(2, 'Anjali', 'MCA', 92),
(3, 'Kiran', 'BSc', 78);

SELECT * FROM student;

-- Create a view
CREATE VIEW student_view AS
SELECT Roll_No, Name, Course
FROM student;

-- Display the view
SELECT * FROM student_view;

-- Show views
SHOW FULL TABLES
WHERE TABLE_TYPE = 'VIEW';

-- Drop the view
DROP VIEW student_view;

-- Verify that the view is removed
SHOW FULL TABLES
WHERE TABLE_TYPE = 'VIEW';

-- Original table still exists
SELECT * FROM student;
```

## Using IF EXISTS

``` sql
DROP VIEW IF EXISTS student_view;
```

### Important Point

After:

``` sql
DROP VIEW student_view;
```

the original `student` table still exists.

------------------------------------------------------------------------

# 4. DROP INDEX

An index improves the speed of data retrieval.

`DROP INDEX` removes an index from a table.

## Syntax

``` sql
DROP INDEX index_name ON table_name;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_index_db;

USE drop_index_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

-- Create an index
CREATE INDEX idx_student_name
ON student(Name);

-- View indexes
SHOW INDEX FROM student;

-- Drop the index
DROP INDEX idx_student_name
ON student;

-- View indexes again
SHOW INDEX FROM student;
```

### Important Point

Dropping an index does **not** delete:

-   The column
-   The table
-   The data

It only removes the index.

------------------------------------------------------------------------

# 5. DROP PRIMARY KEY

A primary key can be removed using `ALTER TABLE`.

## Syntax

``` sql
ALTER TABLE table_name
DROP PRIMARY KEY;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_primary_key_db;

USE drop_primary_key_db;

CREATE TABLE student (
    Roll_No INT PRIMARY KEY,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

SHOW CREATE TABLE student;

-- Drop the primary key
ALTER TABLE student
DROP PRIMARY KEY;

-- Check the result
DESC student;

SHOW CREATE TABLE student;

SELECT * FROM student;
```

### Important Point

The `Roll_No` column is **not deleted**.

Only the primary key constraint is removed.

------------------------------------------------------------------------

# 6. DROP FOREIGN KEY

A foreign key is removed using `ALTER TABLE`.

## Syntax

``` sql
ALTER TABLE child_table
DROP FOREIGN KEY constraint_name;
```

## Complete Practical Example

``` sql
CREATE DATABASE drop_foreign_key_db;

USE drop_foreign_key_db;

-- Parent table
CREATE TABLE department (
    Dept_ID INT PRIMARY KEY,
    Dept_Name VARCHAR(30)
);

-- Child table
CREATE TABLE employee (
    Emp_ID INT,
    Emp_Name VARCHAR(30),
    Dept_ID INT
);

INSERT INTO department VALUES
(1, 'HR'),
(2, 'Developer'),
(3, 'Testing');

INSERT INTO employee VALUES
(101, 'Ravi', 1),
(102, 'Anjali', 2),
(103, 'Kiran', 3);

-- Add foreign key
ALTER TABLE employee
ADD CONSTRAINT fk_employee_department
FOREIGN KEY (Dept_ID)
REFERENCES department(Dept_ID);

-- View foreign key
SHOW CREATE TABLE employee;

-- Drop foreign key
ALTER TABLE employee
DROP FOREIGN KEY fk_employee_department;

-- Verify
SHOW CREATE TABLE employee;

SELECT * FROM employee;
```

### Important Point

The constraint name:

``` text
fk_employee_department
```

is used when dropping the foreign key.

The `Dept_ID` column is **not deleted**.

------------------------------------------------------------------------

# 7. DROP TABLE vs DROP DATABASE vs DROP VIEW vs DROP INDEX

  Command                          What is Removed?
  -------------------------------- -------------------------------------------
  `DROP TABLE`                     Table + all its data
  `DROP DATABASE`                  Database + all objects and data inside it
  `DROP VIEW`                      View only
  `DROP INDEX`                     Index only
  `ALTER TABLE DROP PRIMARY KEY`   Primary key constraint only
  `ALTER TABLE DROP FOREIGN KEY`   Foreign key constraint only

------------------------------------------------------------------------

# 8. DROP vs DELETE vs TRUNCATE

Students should clearly understand the difference.

  Command             Removes Rows   Removes Table Structure   Removes Database
  ----------------- -------------- ------------------------- ------------------
  `DELETE`                     Yes                        No                 No
  `TRUNCATE`              All rows                        No                 No
  `DROP TABLE`                 Yes                       Yes                 No
  `DROP DATABASE`              Yes                       Yes                Yes

------------------------------------------------------------------------

## DELETE

``` sql
DELETE FROM student;
```

Removes rows but keeps the table.

### Example

``` sql
CREATE DATABASE delete_example_db;

USE delete_example_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi'),
(2, 'Anjali'),
(3, 'Kiran');

SELECT * FROM student;

DELETE FROM student
WHERE Roll_No = 2;

SELECT * FROM student;
```

Only the row with `Roll_No = 2` is removed.

------------------------------------------------------------------------

# 9. TRUNCATE

`TRUNCATE TABLE` removes all rows but keeps the table structure.

## Complete Example

``` sql
CREATE DATABASE truncate_example_db;

USE truncate_example_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi'),
(2, 'Anjali'),
(3, 'Kiran');

SELECT * FROM student;

TRUNCATE TABLE student;

SELECT * FROM student;

DESC student;
```

The table still exists, but it contains no rows.

------------------------------------------------------------------------

# 10. DROP TABLE vs TRUNCATE

Before:

``` text
student table
-----------------------
1   Ravi
2   Anjali
3   Kiran
```

After:

``` sql
TRUNCATE TABLE student;
```

Result:

``` text
student table
-----------------------
empty
```

The table still exists.

After:

``` sql
DROP TABLE student;
```

Result:

``` text
student table
-----------------------
does not exist
```

------------------------------------------------------------------------

# 11. Complete DROP Practical Example

This example demonstrates:

1.  Create database
2.  Create table
3.  Insert data
4.  Create index
5.  Drop index
6.  Create view
7.  Drop view
8.  Drop table
9.  Drop database

``` sql
CREATE DATABASE drop_complete_db;

USE drop_complete_db;

-- =========================================
-- STEP 1: CREATE TABLE
-- =========================================

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

-- =========================================
-- STEP 2: INSERT DATA
-- =========================================

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

-- =========================================
-- STEP 3: VIEW DATA
-- =========================================

SELECT * FROM student;

-- =========================================
-- STEP 4: SHOW TABLES
-- =========================================

SHOW TABLES;

-- =========================================
-- STEP 5: CREATE INDEX
-- =========================================

CREATE INDEX idx_student_name
ON student(Name);

-- =========================================
-- STEP 6: VIEW INDEX
-- =========================================

SHOW INDEX FROM student;

-- =========================================
-- STEP 7: DROP INDEX
-- =========================================

DROP INDEX idx_student_name
ON student;

-- =========================================
-- STEP 8: VIEW INDEX AGAIN
-- =========================================

SHOW INDEX FROM student;

-- =========================================
-- STEP 9: CREATE VIEW
-- =========================================

CREATE VIEW student_view AS
SELECT Roll_No, Name, Course
FROM student;

-- =========================================
-- STEP 10: VIEW DATA THROUGH VIEW
-- =========================================

SELECT * FROM student_view;

-- =========================================
-- STEP 11: SHOW VIEWS
-- =========================================

SHOW FULL TABLES
WHERE TABLE_TYPE = 'VIEW';

-- =========================================
-- STEP 12: DROP VIEW
-- =========================================

DROP VIEW student_view;

-- =========================================
-- STEP 13: VERIFY VIEW IS REMOVED
-- =========================================

SHOW FULL TABLES
WHERE TABLE_TYPE = 'VIEW';

-- =========================================
-- STEP 14: ORIGINAL TABLE STILL EXISTS
-- =========================================

SELECT * FROM student;

-- =========================================
-- STEP 15: DROP TABLE
-- =========================================

DROP TABLE student;

-- =========================================
-- STEP 16: VERIFY TABLE IS REMOVED
-- =========================================

SHOW TABLES;

-- =========================================
-- STEP 17: DROP DATABASE
-- =========================================

DROP DATABASE drop_complete_db;
```

------------------------------------------------------------------------

# 12. Complete DROP DATABASE Example

This is the simplest example to demonstrate `DROP DATABASE`.

``` sql
CREATE DATABASE drop_database_practical;

USE drop_database_practical;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

SHOW TABLES;

-- Drop complete database
DROP DATABASE drop_database_practical;
```

After the `DROP DATABASE` command, the database and everything inside it
are removed.

------------------------------------------------------------------------

# 13. Complete DROP TABLE Example

``` sql
CREATE DATABASE drop_table_practical;

USE drop_table_practical;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

SHOW TABLES;

DROP TABLE student;

SHOW TABLES;
```

------------------------------------------------------------------------

# 14. Complete DROP VIEW Example

``` sql
CREATE DATABASE drop_view_practical;

USE drop_view_practical;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30),
    Marks INT
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA', 85),
(2, 'Anjali', 'MCA', 92),
(3, 'Kiran', 'BSc', 78);

CREATE VIEW student_view AS
SELECT Roll_No, Name, Course
FROM student;

SELECT * FROM student_view;

DROP VIEW student_view;

SHOW FULL TABLES
WHERE TABLE_TYPE = 'VIEW';

SELECT * FROM student;
```

The `student` table still exists.

------------------------------------------------------------------------

# 15. Complete DROP INDEX Example

``` sql
CREATE DATABASE drop_index_practical;

USE drop_index_practical;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

CREATE INDEX idx_student_name
ON student(Name);

SHOW INDEX FROM student;

DROP INDEX idx_student_name
ON student;

SHOW INDEX FROM student;

SELECT * FROM student;
```

The table and its data remain unchanged.

------------------------------------------------------------------------

# 16. DROP PRIMARY KEY Example

``` sql
CREATE DATABASE drop_pk_practical;

USE drop_pk_practical;

CREATE TABLE student (
    Roll_No INT PRIMARY KEY,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

SHOW CREATE TABLE student;

ALTER TABLE student
DROP PRIMARY KEY;

SHOW CREATE TABLE student;

SELECT * FROM student;
```

------------------------------------------------------------------------

# 17. DROP FOREIGN KEY Example

``` sql
CREATE DATABASE drop_fk_practical;

USE drop_fk_practical;

CREATE TABLE department (
    Dept_ID INT PRIMARY KEY,
    Dept_Name VARCHAR(30)
);

CREATE TABLE employee (
    Emp_ID INT,
    Emp_Name VARCHAR(30),
    Dept_ID INT
);

INSERT INTO department VALUES
(1, 'HR'),
(2, 'Developer'),
(3, 'Testing');

INSERT INTO employee VALUES
(101, 'Ravi', 1),
(102, 'Anjali', 2),
(103, 'Kiran', 3);

ALTER TABLE employee
ADD CONSTRAINT fk_employee_department
FOREIGN KEY (Dept_ID)
REFERENCES department(Dept_ID);

SHOW CREATE TABLE employee;

ALTER TABLE employee
DROP FOREIGN KEY fk_employee_department;

SHOW CREATE TABLE employee;

SELECT * FROM employee;
```

------------------------------------------------------------------------

# 18. Using IF EXISTS

`IF EXISTS` prevents an error when the object does not exist.

## DROP TABLE

``` sql
DROP TABLE IF EXISTS student;
```

## DROP DATABASE

``` sql
DROP DATABASE IF EXISTS college;
```

## DROP VIEW

``` sql
DROP VIEW IF EXISTS student_view;
```

## Example

``` sql
CREATE DATABASE drop_if_exists_db;

USE drop_if_exists_db;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi'),
(2, 'Anjali');

SELECT * FROM student;

DROP TABLE IF EXISTS student;

-- Running it again does not produce the "table doesn't exist" error
DROP TABLE IF EXISTS student;

DROP DATABASE IF EXISTS drop_if_exists_db;
```

------------------------------------------------------------------------

# 19. DROP Command Quick Reference

  Requirement            MySQL Command
  ---------------------- --------------------------------------------------
  Drop table             `DROP TABLE student;`
  Drop table safely      `DROP TABLE IF EXISTS student;`
  Drop database          `DROP DATABASE college;`
  Drop database safely   `DROP DATABASE IF EXISTS college;`
  Drop view              `DROP VIEW student_view;`
  Drop view safely       `DROP VIEW IF EXISTS student_view;`
  Drop index             `DROP INDEX idx_name ON student;`
  Drop primary key       `ALTER TABLE student DROP PRIMARY KEY;`
  Drop foreign key       `ALTER TABLE employee DROP FOREIGN KEY fk_name;`

------------------------------------------------------------------------

# 20. DROP Command Summary

``` text
DROP TABLE
    ↓
Removes table + data

DROP DATABASE
    ↓
Removes database + tables + data

DROP VIEW
    ↓
Removes view only

DROP INDEX
    ↓
Removes index only

ALTER TABLE ... DROP PRIMARY KEY
    ↓
Removes primary key constraint

ALTER TABLE ... DROP FOREIGN KEY
    ↓
Removes foreign key constraint
```

------------------------------------------------------------------------

# 21. Important Differences

## DROP vs DELETE

``` text
DELETE
→ Removes rows
→ Table remains

DROP TABLE
→ Removes rows
→ Removes table structure
```

## DROP vs TRUNCATE

``` text
TRUNCATE
→ Removes all rows
→ Table remains

DROP TABLE
→ Removes all rows
→ Table is removed
```

## DROP TABLE vs DROP DATABASE

``` text
DROP TABLE
→ Removes one table

DROP DATABASE
→ Removes the complete database
→ Removes all tables and objects inside it
```

------------------------------------------------------------------------

# 22. Recommended Student Practice Order

Students should practice in this order:

``` text
1. CREATE DATABASE
2. USE DATABASE
3. CREATE TABLE
4. INSERT DATA
5. SELECT * FROM table
6. SHOW TABLES
7. DROP TABLE
8. DROP DATABASE
9. CREATE VIEW
10. DROP VIEW
11. CREATE INDEX
12. DROP INDEX
13. DROP PRIMARY KEY
14. DROP FOREIGN KEY
15. Compare DROP vs DELETE
16. Compare DROP vs TRUNCATE
```

For every practical example, students should observe:

``` sql
SELECT * FROM table_name;
```

before the operation and then use:

``` sql
DESC table_name;
```

or:

``` sql
SHOW CREATE TABLE table_name;
```

after the operation to understand exactly what changed.

------------------------------------------------------------------------

# 23. Final Classroom Exercise

Give students the following task without showing the answer first.

## Task

Create a database named `college_drop_exercise`.

1.  Create a `student` table.
2.  Insert three records.
3.  Display all records.
4.  Create an index on `Name`.
5.  Display the index.
6.  Drop the index.
7.  Create a view containing `Roll_No`, `Name`, and `Course`.
8.  Display the view.
9.  Drop the view.
10. Display the original table.
11. Drop the table.
12. Drop the database.

## Answer

``` sql
CREATE DATABASE college_drop_exercise;

USE college_drop_exercise;

CREATE TABLE student (
    Roll_No INT,
    Name VARCHAR(30),
    Course VARCHAR(30)
);

INSERT INTO student VALUES
(1, 'Ravi', 'BCA'),
(2, 'Anjali', 'MCA'),
(3, 'Kiran', 'BSc');

SELECT * FROM student;

CREATE INDEX idx_student_name
ON student(Name);

SHOW INDEX FROM student;

DROP INDEX idx_student_name
ON student;

SHOW INDEX FROM student;

CREATE VIEW student_view AS
SELECT Roll_No, Name, Course
FROM student;

SELECT * FROM student_view;

DROP VIEW student_view;

SELECT * FROM student;

DROP TABLE student;

DROP DATABASE college_drop_exercise;
```
